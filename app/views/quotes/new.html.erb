<main class="w-full pr-1 pl-1 mx-auto">
  <%= link_to sanitize("&larr; Back to quotes"), quotes_path %>

  <div class="flex flex-wrap gap-3">
    <h1 class="text-4xl">New quote</h1>
  </div>
  <%= turbo_frame_tag quote do %>
    <%= render "form", quote: quote %>
  <% end %>
</main>
